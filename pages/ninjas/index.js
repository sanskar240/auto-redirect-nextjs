import next from "next";


const Ninjas = () =>{
    return (
        <div className="not-found">
            <h1 className= {styles.heading}>See all listings</h1>
        </div>
    );
}

export default Ninjas;